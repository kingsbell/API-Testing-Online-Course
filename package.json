{
    "name": "APITestingMocha",
    "version": "1.0.0",
    "description": "",
    "type": "module",
    "scripts": {
        "test": "mocha tests --recursive --timeout 5000 --reporter spec"
    },
    "dependencies": {
        "node-fetch": "^3.3.2",
        "ajv": "^8.16.0"
    },
    "devDependencies": {
        "chai": "^5.1.0",
        "mocha": "^10.5.0",
        "mocha-simple-html-reporter": "^2.0.0"
    }
}